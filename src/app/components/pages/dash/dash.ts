import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-dash',
  imports: [RouterLink],
  templateUrl: './dash.html',
  styleUrl: './dash.css',
})
export class Dash {}
